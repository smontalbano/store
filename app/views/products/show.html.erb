<h1><%= @product.name %></h1>
<% if authenticated? %>
    <button><%= link_to "Edit", edit_product_path%></button>
    <%= button_to "Delete", @product, method: :delete, data: { turbo_confirm: "Are you sure?" } %>
<% end %>
<button><%= link_to "Back", products_path %></button>